# Static Astro output. Phase 1 has no Worker script.
# D1, KV, R2, and Functions are Phase 2 — do not add them here yet.
name = "spec-challenge"
compatibility_date = "2026-08-31"

[assets]
directory = "./dist"
not_found_handling = "404-page"

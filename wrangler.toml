# Cloudflare Pages project. Phase 1 is a static Astro build.
# D1, KV, R2, and Functions are Phase 2 — do not add them here yet.
name = "spec-challenge"
compatibility_date = "2026-08-31"
pages_build_output_dir = "dist"

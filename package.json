{
  "name": "spec-challenge",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix site",
    "build": "npm ci --prefix site && npm run build:cloudflare --prefix site",
    "build:docs": "npm run build:github-pages --prefix site && rm -rf docs && mkdir -p docs && cp -R site/dist/client/. docs/ && touch docs/.nojekyll",
    "preview": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
